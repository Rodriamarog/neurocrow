Jan 17 - Need to confirm if messages are added to the database even though 'bot_enabled' is set to 'false'.
I also need to fix the admin-dashboard thing so that it connects to the correct database now.

